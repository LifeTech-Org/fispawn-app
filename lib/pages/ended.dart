import 'package:flutter/material.dart';

class Ended extends StatelessWidget {
  const Ended({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Game has ended'),
    );
  }
}
